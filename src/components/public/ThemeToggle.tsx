'use client';

/**
 * Theme switching is intentionally disabled because the application uses dark mode only.
 * The component remains exported for compatibility with any external imports.
 */
export default function ThemeToggle() {
  return null;
}
